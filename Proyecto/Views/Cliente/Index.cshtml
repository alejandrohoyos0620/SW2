@model IEnumerable<Proyecto.Models.ClienteModel>

@{
    ViewBag.Title = "Listado de Clientes";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear Nuevo Cliente", "Create")
</p>
<table class="table">
    <tr>
        <th>
            identificacion
        </th>
        <th>
            Nombre
        </th>
        <th>
            Apellido
        </th>

        <th>
            Direccion
        </th>
        <th>
            Correo Electrónico
        </th>
        
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Id1)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Nombre1)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Apellido1)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Direccion1)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Correo1)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.Id1 }) |
        @Html.ActionLink("Details", "Details", new { id = item.Id1 }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.Id1 })
    </td>
</tr>
    }

</table>
